include ':app'
include ':capacitor-cordova-android-plugins'
project(':capacitor-cordova-android-plugins').projectDir = new File('./capacitor-cordova-android-plugins/')

apply from: 'capacitor.settings.gradle'

// capacitor.settings.gradle uses the pnpm virtual store path which only exists
// in the Replit build environment. Override it here with the standard symlinked
// node_modules path so the project builds on any machine after `pnpm install`.
project(':capacitor-android').projectDir = new File('../node_modules/@capacitor/android/capacitor')
